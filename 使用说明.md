# 匿名投票系统 · 使用说明

> 一句话：**每个人先注册（用真名），发起人建投票并导入名单，参与者登录投票，发起人看进度、结束后公布票数。**

---

## 一、给所有人：注册 / 登录 / 改密码

### 注册

1. 打开投票系统网址（发起人会发给你）。
2. 点右上角 **注册**。
3. 填写两样：
   - **真实姓名**：⚠️ 一定要填**真名**（比如"张三"），它**就是你的登录账号**，全系统唯一。系统靠真名把你和投票名单对上，填错就收不到投票邀请。
   - **密码**：自己设，两次一致。只能用大小写字母和数字。
4. 点"注册"，会自动登录，进入你的主页。

> 下次来直接用**真实姓名 + 密码**登录即可（不需要单独的用户名）。

### 改密码

1. 登录后，点顶部导航栏的「**改密码**」。
2. 填三样：
   - **原密码**：你现在用的密码，用来验证是你本人。
   - **新密码**：填两遍，必须一致。规则和注册时一样——**只能大小写字母和数字**。
3. 点「确认修改」，**立即生效**，下次登录请用新密码。

> 忘了密码：找发起人（管理员）处理。管理员自己忘了密码的找回办法见部署文档的「管理员密码」一节。

---

## 二、给发起人：怎么发起一场投票

> ⚠️ 只有管理员账号可以发起和管理投票：登录姓名 **admin**，密码见部署方设置（默认 `muyu123`）。
> 普通账号登录后看不到"发起投票"入口。`admin` 是系统保留姓名，别人无法注册占用。
> 管理员也可以点顶部「改密码」自己改密码（永久生效）；只有在 Render 设了环境变量 `ADMIN_PASSWORD` 时，
> 重启才会把密码校正回该变量的值——这也是忘记密码时的找回通道。

1. 用 **admin** + 管理员密码登录，点顶部 **发起投票**。
2. 填写：
   - **投票标题**：比如"2026 优秀班干部评选"。
   - **投票说明**：可选，补充几句。
   - **投票选项**：填候选人/选项名字。默认 2 个，点"＋ 添加选项"可加更多，也能删。
   - **每人拥有的票数**：默认 **1**（= 单选）。填 **7** 就是**每人 7 票，必须投给 7 个不同的人**。
     ⚠️ 票数不能超过可投对象数——不允许投自己时，可投对象 = 选项数 − 1（每个人都会少掉与自己同名的那一项）。
     所以"每人 7 票"至少要有 **8 个选项**。页面下方会实时提示当前最多能设几票。
3. 两个开关按需勾选：
   - **允许「弃票」**：勾上后，投票者可以选"弃票"——选了就**一票都不投**。
   - **投票者能否投给自己**：默认"否"。选"否"时，选项名与本人真名一致的那个选项会从他的投票页**直接消失**，想投也投不到（后端也会再拦一道）。
4. **导入投票者名单**（发起人自己不算在内，不能投自己发起的票）：
   - 方式一：在文本框里**每行一个姓名**，或用逗号/顿号隔开。
   - 方式二：上传 **Excel / CSV**，系统自动读第一列的姓名（会自动跳过"姓名"这类表头）。
   - 图片名单：系统无法自动识别文字，请照着图片**手动录入**到文本框。
   - ⚠️ 名单里的姓名必须是大家注册时填的**真实姓名**，才能对上号。
5. 点 **创建投票**，进入管理页。

---

## 三、给发起人：管理页能干什么

创建后你会进入这场投票的**管理页**，功能有：

- **投票进度（自动刷新）**：不用手动刷新，页面每几秒自动更新，实时显示：
  - 应投人数 / 已投票 / 未投票 /（含弃票时）其中弃票；
  - **尚未投票**的名单（黄色）——催票很方便；
  - **已投票**的名单（绿色），弃票的人会额外标注"**· 弃票**"。
  - 名字后面带 `*` 的，表示这个人**还没注册账号**；等他用相同真名注册后会自动关联。
- **导出 Excel**（右上角按钮，随时可点，投票进行中也能导）：
  - **得票统计**：按票数从高到低排列，**票数并列的名次也并列**（如 6 人同为 2 票则并列第 1，下一名直接跳到第 7）；弃票单独一行，不计入任何人的得票。
  - **投票明细**：每个人一行——姓名 / 状态（已投票·弃票·未投票）/ **投票时间** / **投给了谁**。
  - **投票矩阵**：行 = 投票人，列 = 候选人，打勾看得最直观。
- **追加投票者名单**：临时想加人，随时用文本或 Excel 追加。
- **票数明细**（只有发起人看得到）：
  - 显示每个选项的票数和占比条；
  - 右上角有个**「显示投票者名字」开关**：打开后，每个选项后面会列出是谁投的；关闭则只看票数。
- **结束投票**：点红色"结束投票"，投票者就不能再投了。
- **公布票数**：投票结束后，可以点"公布票数 / 取消公布"。

---

## 四、给参与者：怎么投票

1. 用你的账号登录。
2. 主页会有 **「邀请我参与的投票」** 一栏，点进对应投票的 **去投票**。
   - 如果没看到：确认你注册时填的**真实姓名**和发起人名单里写的一致。
3. 按页面提示选人：
   - 页面顶部会写清**本次你要投几票**，必须**投满**才能提交（按钮上会实时显示"还需再选 N 人"）；
   - **不能重复投给同一个人**，超选会当场拦下；
   - **投不到自己**：与你同名的选项根本不会出现在列表里；
   - 若本场允许弃票，列表最下面有一项「**弃票**」：勾上它，上面的选项会被清空并锁定，等于**一票都不投**；弃票与投票只能二选一。
4. 点 **提交投票**（投完不能改，想好再投）。
5. 投完后你**只能看到自己投给了谁**（弃票就显示"你已选择弃票"），看不到别人的选择、也看不到各选项票数——这是为了保护匿名。

---

## 五、匿名规则（重要）

- **参与者之间完全匿名**：任何投票者都**看不到**谁投了谁，也看不到票数和结果。
- **发起人可以看明细**：发起人（以及系统管理员）能在管理页看到谁投了哪个选项。
  - 这是当初确认的规则："对参与者匿名，发起人可看明细"。
  - 如果你希望连发起人也不做展示，发起人只要**关闭"显示投票者名字"开关**，页面上就只显示票数、不显示姓名。

---

## 六、常见问题

**Q：参与者说主页看不到被邀请的投票？**
A：99% 是真实姓名对不上。让他确认注册时填的真名，和发起人名单里的写法完全一致（别多空格、别用昵称）。

**Q：第一次打开网站很慢、转很久？**
A：免费服务器闲置约 15 分钟会"休眠"，下次访问需要 30–60 秒唤醒，属正常现象。数据不会丢。

**Q：发起人能投票吗？**
A：不能。发起人不参与自己发起的投票，也不会出现在名单里。

**Q：投错了能改吗？**
A：不能。每人每场只能投一次，提交后不可修改，所以提交前会有确认提示。

**Q：投票结束后还能改结果吗？**
A：结束后无法再投票。发起人可以选择公布或不公布票数。

**Q：我要投 7 票，但只想投 3 个人，行吗？**
A：不行，必须投满。如果这场投票**同时开启了弃票**，不想投就选「弃票」（等于一票不投）。不想强制投满的话，把"每人票数"直接设成你期望的数量。

**Q：为什么我的选项列表里少了一个人？**
A：那多半就是你自己。本场不允许投自己时，与你同名的那一项会从你的投票页隐藏。

**Q：升级新版本后，之前的投票数据还在吗？会不会冲突？**
A：都在、也不会冲突。新版本启动时会自动给旧数据库补字段、放宽约束（幂等、不删任何数据），历史投票/名单/选票全部保留；**旧投票仍然按原来的"单选"规则运行**。

---

## 七、给管理者：日常小贴士

- **催票**：管理页"尚未投票"名单实时更新，一眼看清谁还没投。
- **加人**：随时用"追加投票者名单"补人。
- **留档**：投票结束后点右上角「**导出 Excel**」，把统计+明细+时间存下来；免费数据库有 30 天有效期，导出是最稳的备份方式。
- **保密展示**：给别人看结果时，先关掉"显示投票者名字"开关，只露票数。
- **数据安全**：所有数据存在云数据库里，重启/重新部署都不会丢。
